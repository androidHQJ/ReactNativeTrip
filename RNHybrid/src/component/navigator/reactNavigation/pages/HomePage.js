import React, { Component } from 'react';
import CommonPage from './../../../../common/CommonPage'

type Props = {};
export default class HomePage extends Component<Props> {
    render() {
        return <CommonPage name={'HomePage'}/>
    }
}